# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

type: charm
platforms:
  ubuntu@22.04:amd64:
  ubuntu@22.04:arm64:

parts:
  charm:
    charm-binary-python-packages:
      - psycopg[binary]

config:
  options:
    send-ca-cert:
      description: Send the CA certificate instead of end-entity certificate
      type: boolean
      default: false
