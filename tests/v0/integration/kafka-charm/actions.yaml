# Copyright 2022 Canonical Ltd.
sync-password:
  description: Set password.
  params:
    password:
      type: string
      description: Kafka password.
  required: [password]
sync-username:
  description: Set usernmae.
  params:
    username:
      type: string
      description: Kafka username.
  required: [username]

sync-bootstrap-server:
  description: Set bootstrap-server.
  params:
    bootstrap-server:
      type: string
      description: Kafka bootstrap-server.
  required: [bootstrap-server]


